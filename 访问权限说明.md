# 外汇与贵金属预测站访问权限

线上地址：<https://fx-metals-forecast-board.aspttwwly888.workers.dev/USDCNH>

## 公开内容

- `01 Observatory`
- 每个品种的基础预测页面
- 基础预测 JSON 和普通预测下载文件

知道网址即可访问，不需要用户名或密码。

## 需要登录的内容

- `02 Constellation`
- `03 Terrain`
- `/data/terrain/` 下的历史地形 JSON
- `latest_terrain_snapshot.xlsx`
- `latest_trade_signals.xlsx`

点击 02 或 03 时浏览器会要求输入用户名和密码。同一浏览器会话内验证一次后，02 和 03 可以共同使用。

## 安全实现

访问控制在 Cloudflare Worker 中执行。密码保存在 Worker Secret `SITE_PASSWORD` 中，不写入 HTML、JavaScript、构建目录或 Git 仓库。

如需修改密码，在本目录运行：

```powershell
npx wrangler secret put SITE_PASSWORD
```

输入新密码后重新打开浏览器验证即可，不需要重新生成预测数据。
